version: 1.0
base: node:24
build: npm install --production && npm i --save-dev @types/node && npm run build
run: node .
metadata_schema:
  schema:
    name: metadata_schema
    properties:
      last_pokemon_id:
        type: number
