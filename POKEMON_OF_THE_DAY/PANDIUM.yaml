version: 0.4
base: node:20.9.0
build: npm install --production && npm i --save-dev @types/node && npm run build
run: node .
configs:
  schema:
    properties:
      placeholder:
          type: string
    type: object 
  uischema:
    elements:
    - type: Section
      label: Pokémon of the Day
      hintText: No Configuration required.
    type: VerticalLayout
