import * as hubspot from '@hubspot/api-client';
export declare class HubspotClient {
    private errorCount;
    private baseUrl?;
    private abortController;
    api: hubspot.Client;
    constructor(abortController: AbortController, baseUrl?: string, apiToken?: string, apiKey?: string);
}
