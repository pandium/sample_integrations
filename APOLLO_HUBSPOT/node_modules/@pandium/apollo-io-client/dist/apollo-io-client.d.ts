import { endpoints } from './apollo-io.js';
import { ZodiosOptions } from "@zodios/core";
import { PandiosInstance } from "./pandios.js";
export default class ApolloClient {
    private axiosInstance;
    private errorCount;
    private baseUrl;
    private abortController;
    api: PandiosInstance<typeof endpoints>;
    private bucket;
    constructor(baseUrl: string, abortController: AbortController, apiToken?: string, options?: ZodiosOptions);
}
